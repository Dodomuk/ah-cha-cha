# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# production
/build

# misc
.DS_Store
Thumbs.db
*.pem

# IDE
.vscode/
.idea/

# Claude Code — 로컬 권한 설정이 들어가므로 커밋하지 않는다
.claude/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env — 절대 커밋 금지
.env
.env.*
!.env.example

# vercel
.vercel

# 로컬 피드 캐시 (OpenPhish / URLhaus 등)
/data/feeds/
/logs/

# 교차검증 코퍼스와 결과. 악성 URL 원문이 그대로 들어 있으므로 커밋하지 않는다
/bench/data/
/bench/out/

# 마스코트 중간 산출물. 서빙은 .webp로 하고, .png는 원본 시트에서
# scripts/slice-mascot.py 로 언제든 다시 만들 수 있다 (PNG가 WebP의 8배 용량)
/public/mascot/poses/*.png
