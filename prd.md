# 아차차 (ahchacha.com) — MVP 기획서

> 악성 웹/앱 판별 서비스. **처음 보는 링크를 대신 열어보고 판정한다.**
> 개발 규칙 요약본은 [CLAUDE.md](CLAUDE.md) 참조.

**문서 버전** v2.1 (2026-08-16) — 실측 결과로 서비스 정의 수정 (0.1절)
**이전 버전** v2.0 (2026-08-15) — 기존 IT 뉴스/증시 서비스에서 전면 전환
**이전 서비스 코드** `legacy` 브랜치 (`057d5d3`)에 보존

---

## 0.1 서비스 정의 — 우리가 파는 것은 목록이 아니라 판정이다

> 이 절은 v2.1에서 추가됐다. v2.0은 "사용자 신고 기반 DB + 자동 스캔 엔진
> 하이브리드"라고 정의했는데, **실측해보니 신고 DB를 앞세우는 것은 틀린
> 포지셔닝이었다.** 근거는 [bench/README.md](bench/README.md)에 있다.

### 왜 "신고 DB"가 주력이 될 수 없는가

KISA 피싱 URL 13만 건(2024년)을 수명 기준으로 세어봤다.

| 목록에 남아 있던 기간 | URL | 등록도메인 |
| --- | --- | --- |
| 하루 | **42.1%** | 23.1% |
| 2~7일 | 24.8% | 10.9% |
| 8~30일 | 15.8% | 23.5% |
| 31일 이상 | 17.4% | **42.5%** |

정부24 사칭 캠페인을 뜯어보면 더 분명하다. **URL 1,129개가 등록도메인
1,099개를 쓴다** — 거의 한 번 쓰고 버린다. 도메인 1,099개를 전부 신고받아도
1,100번째는 못 막는다.

목록 방식의 한계는 남들에게도 똑같다. 실측에서 **구글 Safe Browsing이 URLhaus
100건 중 0건**을 알고 있었다. 세계 최대 블록리스트도 남의 목록을 못 따라간다.
목록은 원리상 항상 뒤에 선다.

### 그런데 패턴은 죽지 않는다

버려진 도메인 1,099개가 **전부 같은 모양이었다**: `gov.` + 4글자 무작위 라벨 +
싸구려 TLD(`.work`, `.icu`, `.lat`, `.one`…). 규칙 하나가 1,099개를 전부 덮고,
아직 나오지 않은 1,100번째도 덮는다.

**아차차의 자산은 쌓인 URL 목록이 아니라 "한국 스미싱의 생김새를 아는 판정
규칙"이다.** URL이 죽어도 규칙은 안 죽는다.

### 정리 — 주력과 보조

```
주력   처음 보는 링크를 즉시 열어보고 판정한다
       어느 목록에도 없는 주소여야 의미가 있다

보조   사용자 신고는 두 가지 일을 한다
       (a) 캠페인 안에서 다른 피해자에게 알린다
       (b) 우리 엔진의 미탐을 찾아준다  ← 이쪽이 더 크다
```

**(a)에 대하여**: 스미싱은 같은 URL을 수천 명에게 몇 시간 안에 뿌린다.
1번 수신자가 아침에 신고하면 2~5000번 수신자가 그 하루 안에 검사한다.
42%가 하루만 산다는 것은 신고가 쓸모없다는 뜻이 아니라, **그 하루가 정확히
피해가 나는 하루**라는 뜻이다. 신고의 가치는 시간을 가로지르는 데 있지 않고
캠페인 안에 있다.

**(b)에 대하여**: "엔진은 `no_signal`을 줬는데 사용자는 피싱이라고 신고했다"는
우리가 가질 수 있는 가장 비싼 정보다. 공개 코퍼스가 주지 못하는 것 —
**살아 있는 한국어 표본** — 을 신고가 준다. 신고가 판정을 바꾸지 않는 현재
구조(규칙 8)가 오히려 이 용도에 맞다. 둘이 섞이지 않아야 불일치가 개선
신호로 남는다.

**신고는 도메인 단위로 쌓는다.** URL 단위로 쌓으면 42%가 하루 만에 무의미해지고,
도메인 단위로 쌓으면 42.5%가 한 달 넘게 유효하다. `domains.report_count`가
이미 도메인 단위인 것은 이 점에서 맞는 설계다.

---

## 0. 네이밍 & 브랜드 전제

`ahchacha` = **아차차**. "아차!" 하는 순간 — 링크를 누르기 직전 혹은 누른 직후의 그 감정 — 을 그대로 담고 있어서, 원래 다른 목적으로 산 도메인이지만 이 서비스에 오히려 정확히 맞음.

- **서비스명**: 아차차
- **마스코트**: `차차` — 사용자를 대신해 위험한 링크에 먼저 뛰어들어 보고 오는 캐릭터
- **한 줄 정의**: "누르기 전에, 차차한테 먼저 물어보세요"
- **톤 원칙 (가장 중요)**
  - 안전/보통 → 차차가 전면에 나와 귀엽게 설명
  - **위험 → 차차는 화면에서 물러나고, 경고가 전면을 차지**
  - 귀여움은 *진입장벽을 낮추는 장치*이지, 경고를 부드럽게 만드는 장치가 아님. 이 원칙을 UI 구현 내내 유지할 것.

**마스코트 애셋**: 사용자가 추후 제공 예정. 그 전까지는 `public/mascot/` 에 플레이스홀더를 두고 컴포넌트 인터페이스만 확정한다.

---

## 1. 제품 범위

### 1.1 MVP (Phase 1) — 웹 URL 판별

| 포함 | 제외 |
| --- | --- |
| URL 입력 → 자동 스캔 → 판정 + 자연어 설명 | APK 파일 업로드/분석 |
| 리디렉션 체인 추적 (단축 URL 대응) | 회원 등급/포인트 시스템 |
| 사용자 신고 등록 & 건수 표시 (도메인 단위, 0.1절 참조) | 실시간 알림/푸시 |
| 결과 공유 카드 이미지 생성 | 브라우저 확장 프로그램 |
| 익명 사용 (로그인 없이 검사·신고) | 결제/유료 플랜 |
| 카카오톡 채널 챗봇 (문자 붙여넣기 → 검사) | 비즈니스 채널(사업자등록 필요) |

### 1.2 Phase 2 — 앱(모바일) 판별

- 스토어 링크 / 패키지명 조회 → 알려진 악성앱 DB 대조
- **문자로 받은 APK 다운로드 링크** 검사 (한국 스미싱 시나리오의 핵심)
- 앱 상세: 요구 권한 목록, 개발자 정보, 스토어 등재 여부

### 1.3 Phase 3 — 앱 정적 분석

- MobSF self-host 연동 (Docker, REST API)
- 하드코딩 시크릿, 평문 통신, 노출 컴포넌트, 내장 트래커 리포트
- ⚠️ APK 업로드 방식은 저작권·역공학 이슈가 있으므로 **법률 검토 후 진행**. Phase 2까지는 "링크·패키지명 조회" 범위를 넘지 않는다.

---

## 2. 기술 스택 & 도메인 구조

### 2.1 스택 선정

```
Frontend / Web   : Next.js 16 (App Router) + TypeScript + Tailwind v4
Mobile Wrapper   : Capacitor 7 (동일 코드베이스 재사용)
Backend API      : Next.js Route Handlers (MVP) → 부하 시 별도 분리
Scan Worker      : 별도 격리 컨테이너 (Cloud Run / Fly.io)
DB               : PostgreSQL (Supabase 또는 Neon)
Cache / Queue    : Redis (Upstash)
LLM              : Claude API (설명 생성 레이어)
Hosting          : Vercel (웹) + 워커는 별도
```

> 기획 원안은 Next.js 15였으나 최신 안정 버전인 16.3.1로 시작한다. App Router API는 동일하다.

**Capacitor를 고른 이유**: 1인 개발 + Claude agent 환경에서 코드베이스를 하나로 유지하는 게 가장 중요. Expo/RN은 웹과 코드가 갈라짐.

**⚠️ 앱스토어 심사 리스크**: 웹뷰만 감싼 앱은 Apple 가이드라인 4.2(minimum functionality)로 반려될 수 있음. 앱 버전은 반드시 아래 네이티브 기능을 포함할 것 — 이게 앱을 내는 이유이기도 함.

1. **공유 시트 확장** (카카오톡/문자에서 링크 공유 → 바로 검사) ← 앱의 존재 이유
2. 클립보드 감지 (앱 진입 시 복사된 URL 자동 제안)
3. 로컬 검사 기록 (오프라인 조회)

### 2.2 서브도메인 설계

| 도메인 | 용도 |
| --- | --- |
| `ahchacha.com` | 메인 웹 서비스 |
| `ahchacha.com/s/{shortId}` | 검사 결과 공유 링크 |
| `api.ahchacha.com` | API (Phase 1은 `/api` 라우트로 시작, 분리는 나중에) |
| `scan.ahchacha.com` | **격리된 스캔 워커.** 의심 URL 접속·스크린샷 전담 |
| `cdn.ahchacha.com` | 스크린샷/공유카드 이미지 정적 서빙 |

**스캔 워커를 반드시 분리하는 이유**: 악성 URL에 실제로 접속하는 주체이므로, 메인 서비스 인프라와 네트워크·자격증명을 완전히 격리해야 함. 워커에는 DB 쓰기 권한을 주지 말고 결과를 큐로 반환.

### 2.3 저장소 구조

루트 평탄화. Vercel Root Directory 설정을 `frontend` → `.` 로 변경해야 함.

```
app/            Next.js App Router
lib/scanner/    스캔 엔진 (서버 전용)
worker/         (Phase 3) 격리 스캔 워커
public/mascot/  차차 애셋
```

---

## 3. 탐지 엔진 설계

### 3.1 시그널 소스 (MVP)

| # | 시그널 | 소스 | 비용 | 가중치 |
| --- | --- | --- | --- | --- |
| S1 | 알려진 악성 URL | Google Safe Browsing API | 무료 | **최상 (즉시 위험)** |
| S2 | 피싱 피드 대조 | OpenPhish / PhishTank | 무료 | **최상** |
| S3 | 멀웨어 배포 도메인 | URLhaus (abuse.ch) | 무료 | **최상** |
| S4 | 도메인 등록 나이 | IANA RDAP | 무료 | 높음 (30일 미만 = 강한 위험 신호) |
| S5 | 리디렉션 체인 | 자체 (서버사이드, 최대 10홉) | - | 중 |
| S6 | TLS 인증서 | 자체 (발급일/발급자/SAN) | - | 중 |
| S7 | 브랜드 유사 도메인 | 자체 휴리스틱 | - | 높음 |
| S8 | APK 직접 다운로드 유도 | 자체 (Content-Type / 확장자) | - | **최상 (한국 스미싱 핵심)** |
| S9 | 사용자 신고 누적 | 자체 DB | - | 참고 표시만 (자동판정 미반영) |
| S10 | 다중 백신 스캔 | VirusTotal API | 무료 4회/분 | 중 (비동기 큐로 처리) |

### 3.2 한국 특화 휴리스틱 (S7 / S8) — 최대 차별점

글로벌 도구들이 놓치는 영역. 여기가 실질적 경쟁력.

**브랜드 사칭 사전 구축**: 시중은행, 카드사, 택배사(CJ대한통운·롯데·한진·우체국), 통신 3사, 정부기관(정부24·국민비서·경찰청·건강보험공단), 주요 커머스(쿠팡·네이버·11번가)의 **정식 도메인 화이트리스트**를 먼저 만들고,

- Levenshtein distance ≤ 2 인 유사 도메인 탐지
- Homoglyph 치환 탐지 (`rn`→`m`, `0`→`o`, `1`→`l`, 키릴 문자 혼입)
- 서브도메인 사칭 탐지 (`kbstar.evil.com` 처럼 정식 브랜드가 서브도메인에만 존재)
- 브랜드명이 경로/쿼리에만 있고 도메인엔 없는 경우

**APK 유도 탐지**: 최종 목적지의 `Content-Type: application/vnd.android.package-archive` 또는 `.apk` 확장자 → 스토어 밖 APK 배포는 그 자체로 최고 위험. 이건 다른 서비스가 거의 안 잡는 영역인데 한국 스미싱 피해의 주 경로.

### 3.3 판정 체계 — 4단계

```
🚨 danger      : S1~S3 히트 또는 S8 히트 또는 S7 고신뢰 매칭
⚠️ caution     : S4(30일 미만) + 다른 시그널 1개 이상
🤔 unknown     : 응답 없음 / 접속 불가 / 시그널 부족
✅ no_signal   : 확인된 위험 신호 없음
```

**문구 원칙 (법적 방어 + 정직성)**

- ❌ "안전한 사이트입니다"
- ✅ "지금 확인한 범위에서는 위험 신호가 없었어요"

새 피싱 페이지는 계속 생기므로 clean 결과는 어디까지나 *검사 시점 기준*임을 결과 화면에 항상 병기.

### 3.4 LLM 설명 레이어 — 핵심 차별점

raw 시그널 → Claude API → 일반 사용자용 한국어 설명 변환.

```
입력: { verdict, signals[], domain_age_days, redirect_chain[],
        brand_match, final_content_type, tls_info }
출력: { headline (1줄), reasons[] (2~4개, 각 1문장),
        action (사용자가 지금 할 일 1문장) }
```

**프롬프트 원칙**

- 시그널에 없는 내용을 추측하지 말 것 (환각 금지, 근거 없는 단정 금지)
- 전문용어 금지 (`RDAP`, `TLS`, `CSP` → 사용자에게 노출 안 함)
- 위험 판정 시 톤을 부드럽게 만들지 말 것
- 출력은 JSON only

> "위험도 87점" ❌
> "이 사이트는 만든 지 6일밖에 안 됐고, 로그인 화면이 진짜 은행 페이지를 그대로 복사해왔어요. 비밀번호를 넣으면 그대로 넘어갑니다." ✅

LLM 실패 시 템플릿 기반 폴백 문구를 반드시 준비 (LLM은 부가 레이어이지 필수 경로가 아니어야 함).

---

## 4. 보안 설계 — 협상 불가 항목

이 서비스는 "악성 URL을 일부러 접속하는 서비스"이므로 스스로가 공격 표면이 된다.

1. **클라이언트에서 의심 URL을 절대 fetch하지 않는다.** 모든 접속은 서버사이드.
2. **SSRF 방어**: 리디렉션 체인의 *매 홉마다* 목적지 IP를 재검증. 사설 대역(10/8, 172.16/12, 192.168/16, 127/8, 169.254/16, IPv6 대응) 차단. DNS rebinding 대비해 resolve된 IP로 직접 연결.
3. **스캔 워커 격리**: 읽기 전용 컨테이너, 요청마다 새 인스턴스, 15초 타임아웃, 아웃바운드 화이트리스트 없음(=인바운드 차단), 자격증명 미탑재.
4. **캐시 TTL을 짧게 (6시간)**: 피싱 사이트는 클로킹(방문자에 따라 다른 페이지)을 하므로 오래된 clean 결과가 더 위험함.
5. **Rate limit**: IP 기준 검사 10회/분, 신고 5회/시간.
6. **스크린샷 저장 시 개인정보 유의**: 검사 대상 페이지에 타인의 정보가 담길 수 있음. 자동 마스킹은 어렵더라도 신고·삭제 요청 경로를 둘 것.

---

## 5. 사용자 신고 시스템 — 법적 리스크 관리

더콜 방식을 그대로 가져오되, URL은 전화번호보다 명예훼손 리스크가 큼. **실존 사업자를 "사기"로 표시했다가 오탐이면 법적 분쟁이 된다.**

### 설계 원칙

- **판정과 신고를 분리해서 표시한다.**
  - 판정 = 엔진 결과 (객관적 근거 기반)
  - 신고 = "사용자 신고 N건" 이라는 *사실*의 표시 (판정에 자동 반영 X)
- 신고 카테고리: `피싱/가짜사이트` `악성앱 설치 유도` `사기 쇼핑몰` `도박/불법` `스팸` `오탐 신고(안전한데 위험으로 나옴)`
- **이의제기(소명) 창구를 반드시 만든다.** 사이트 운영자가 삭제·정정 요청할 수 있는 경로. 푸터에 상시 노출.
- 신고 임계치 도달 시 자동 노출하지 말고 **관리자 검토 큐**로 보낼 것 (MVP에선 본인이 직접 검토).
- 서비스 이용약관에 "검사 결과는 참고 정보이며 법적 판단이 아님" 명시.

---

## 6. 화면 흐름

### 6.1 전체 플로우

```
[진입]
 ├─ 웹: ahchacha.com 직접 방문
 ├─ 앱: 카카오톡/문자에서 링크 공유 → 공유시트 → 아차차
 └─ 앱: 앱 진입 시 클립보드 URL 감지 → "이거 검사할까요?"
        │
        ▼
   ① 홈 (입력)
        │
        ▼
   ② 검사 중 (2~8초)
        │
        ▼
   ③ 결과
        ├─ 근거 더보기 → ④ 상세 리포트
        ├─ 신고하기   → ⑤ 신고 폼
        ├─ 공유하기   → 공유 카드 이미지 생성
        └─ 내 기록    → ⑥ 검사 기록
```

### 6.2 화면별 명세

#### ① 홈

- 상단: 차차 캐릭터 (idle 애니메이션) + "누르기 전에, 차차한테 먼저 물어보세요"
- 중앙: **URL 입력창 하나.** 다른 요소로 시선 분산시키지 말 것
  - placeholder: `의심스러운 링크를 붙여넣어 주세요`
  - 클립보드에 URL이 있으면 입력창 아래에 "붙여넣기" 칩 노출
- 하단: 최근 신고된 사이트 피드 (도메인은 부분 마스킹, 클릭 불가 — **절대 링크로 만들지 말 것**)
- 앱 전용: 하단 탭바 (검사 / 기록 / 피드 / 설정)

#### ② 검사 중

- 차차가 링크 안으로 뛰어들어가는 애니메이션
- 진행 단계를 텍스트로 노출 (체감 시간 단축 + 신뢰도 상승)
  ```
  주소를 따라가 보는 중…
  이 사이트가 언제 만들어졌는지 확인 중…
  위험 목록과 대조하는 중…
  차차가 정리하고 있어요…
  ```
- 8초 초과 시: "시간이 걸리네요. 결과가 나오면 알려드릴까요?" (앱=푸시, 웹=결과 URL 복사)

#### ③ 결과 — 판정별로 레이아웃이 다름

**✅ 이상 없음 / ⚠️ 주의**

- 차차 캐릭터 전면 (표정 변화: 안심 / 갸웃)
- 판정 배지 + 헤드라인 1줄
- 근거 2~4개 (아이콘 + 1문장)
- "그래도 조심할 점" 안내
- 스크린샷 썸네일 (탭하면 확대, 원본 링크 클릭 불가)

**🚨 위험**

- **캐릭터를 상단 구석으로 축소하거나 제거.** 붉은 경고 영역이 화면 상단 절반 차지
- 헤드라인: 무엇이 위험한지 직설적으로
- "지금 할 일" 액션 카드
  - 링크를 눌렀다면? → 대처 단계 안내
  - 이미 정보를 입력했다면? → 금융감독원 1332 / 경찰청 112 안내
  - APK를 설치했다면? → 삭제 절차 + 다운로드 폴더 APK 파일까지 삭제 안내
- 스크린샷은 **기본 블러 처리**, 탭해야 표시 (진짜 피싱 화면을 무심코 학습하지 않도록)

**공통 하단**

- `검사 시점: 2026-08-15 14:32` + "새 위험 사이트는 계속 생겨요" 고지
- [신고하기] [공유하기] [다시 검사]

#### ④ 상세 리포트 (고급 모드)

기본은 접힘. 개발자·보안 관심층용.

- 리디렉션 체인 전체 (각 홉 도메인/상태코드)
- 도메인 등록일, 등록기관, TLS 발급자·발급일
- 각 시그널 소스별 응답 원문
- 로드된 외부 스크립트 도메인 목록

#### ⑤ 신고 폼

- 카테고리 선택 (필수) → 상세 설명 (선택, 200자) → 캡차 → 제출
- 로그인 불필요. 남용 방지는 rate limit + 캡차로
- 제출 후: "차차가 확인해볼게요. 같은 신고가 모이면 다른 사람에게도 경고가 떠요"

#### ⑥ 검사 기록

- 로컬 저장 (익명 기준). 로그인 도입 시 서버 동기화
- 날짜별 + 판정 배지
- 지난 검사가 이후에 위험으로 바뀌면 뱃지 표시 ("이 사이트, 그 뒤에 위험으로 확인됐어요") ← 재방문 유도 포인트

### 6.3 공유 카드

결과를 이미지로 만들어 카톡으로 보내는 기능. **부모님·지인에게 경고를 전달하는 경로**라 리텐션·바이럴 양쪽에 중요.

- OG 이미지 동적 생성 (`@vercel/og`)
- 카드 내용: 판정 배지 + 도메인(부분 마스킹) + 핵심 근거 1줄 + 아차차 로고
- 카드에는 **실제 URL을 클릭 가능한 형태로 넣지 않는다**

---

## 7. 데이터 모델 (초안)

```sql
scans          -- 검사 실행 이력
  id, url_hash, normalized_url, final_url, verdict,
  signals JSONB, redirect_chain JSONB, screenshot_path,
  llm_explanation JSONB, scanned_at, expires_at

domains        -- 도메인 단위 집계
  id, domain, first_seen, registered_at, current_verdict,
  report_count, admin_reviewed_at, appeal_status

reports        -- 사용자 신고
  id, domain_id, category, description, reporter_ip_hash,
  created_at, review_status

brand_whitelist -- 사칭 탐지용 정식 도메인
  id, brand_name, category, official_domains TEXT[]

appeals        -- 이의제기
  id, domain_id, contact, claim, status, created_at
```

`url_hash`는 정규화된 URL의 SHA-256. 캐시 키 겸 인덱스.

---

## 8. 개발 로드맵

### Sprint 1 — 뼈대와 엔진 ✅ 완료

1. ✅ Next.js + TypeScript + Tailwind 초기화 (루트 평탄화)
2. ✅ `lib/scanner/guard.ts` — SSRF 가드
3. ✅ 리디렉션 체인 추적기
4. ✅ RDAP 도메인 나이 조회
5. ✅ Google Safe Browsing 연동
6. ✅ `POST /api/scan` — 판정 반환 (UI 없이 JSON으로 검증)

### Sprint 2 — 판정 고도화 + 기본 UI (1~2주)

**착수 시 확정된 결정 (2026-08-15)**

| 결정 사항 | 확정 내용 |
| --- | --- |
| APK 오탐 보정 | 정식 스토어만 좁게 예외. 신뢰 호스트는 `danger` → `caution` |
| 피드 저장소 | Upstash Redis 도입. rate limit도 인메모리에서 함께 이관 |
| LLM 모델 | Claude Haiku 4.5 |
| 검사 중 화면 | 클라이언트 타이머로 문구 순환 (API는 단일 JSON 응답 유지) |

7. ✅ **OpenPhish / URLhaus 피드 동기화 (S2·S3)** — cron으로 주기 수집 → Upstash Redis 저장.
   PhishTank는 **제외한다**: 신규 API 키 발급이 사실상 중단됐고 OpenPhish와 커버리지가 겹친다.
   대조는 두 갈래로 나눈다 — URL 완전일치는 `danger`, 호스트 단위 일치는 `caution`.
   공유 호스팅에 피싱 페이지 하나가 올라간 경우 호스트 전체를 위험으로 몰면 오탐이 된다.
8. ✅ **브랜드 화이트리스트 + 유사 도메인 휴리스틱 (S7)** — 시작 범위는 피해 규모 순으로
   은행·카드사·택배사·정부기관·통신 3사. 커머스는 후순위.
   신뢰도를 둘로 나눠 **고신뢰만 `danger`**, 나머지는 `caution`으로 둔다.
   ⚠️ 화이트리스트의 정식 도메인은 **운영 반영 전 반드시 사람이 검증할 것.**
   목록이 틀리면 정상 사이트를 위험으로 몰거나(오탐) 사칭을 통과시킨다(우회).
9. ✅ **APK 유도 탐지 오탐 보정** — 신뢰 호스트 목록(`lib/scanner/allowlist.ts`)에 있는
   정식 스토어에서 받는 APK만 `caution`으로 낮춘다. 그 외 모든 스토어 밖 APK 배포는
   `danger` 유지. **GitHub Releases처럼 공격자도 흔히 쓰는 호스트는 넣지 않는다** —
   화이트리스트가 곧 우회 경로가 되기 때문.
10. ✅ **홈 / 검사중 / 결과 화면 구현** — 스크린샷은 Sprint 3(격리 워커)이므로 이번 결과
    화면에는 포함하지 않았다. 신고·공유 버튼도 각각 Sprint 3 항목 12·14라 아직 없다.
    결과 화면은 `danger`와 그 외로 레이아웃이 갈리며(`components/result-view.tsx`),
    **두 분기를 하나로 합치지 말 것** — 합치는 순간 위험 화면에 마스코트가 돌아온다.
11. ✅ **Claude 설명 레이어 + 템플릿 폴백** — `lib/scanner/claude.ts`를 `explain.ts` 옆에 붙였다.
    Haiku 4.5 + 구조화 출력(`output_config.format`)이라 JSON 파싱 실패 자체가 없다.
    타임아웃 5초, 재시도 없음. 실패·지연 시 템플릿 폴백으로 떨어지며 판정은 그대로 나온다.
    ⚠️ 프롬프트에 **"action에서 방문을 권하거나 보증하지 말 것"**을 명시했다.
    초기 버전이 `no_signal`에서 "평소처럼 이용해도 괜찮아요"를 출력해 7절 문구 원칙을
    정면으로 위반했다. 프롬프트를 고칠 때 이 제약을 지울 것.

### Sprint 3 — 커뮤니티 + 공유 (1~2주)

**착수 시 확정된 결정 (2026-08-15)**

| 결정 사항 | 확정 내용 |
| --- | --- |
| 주소 저장 | 위험한 것만 원문 보관. `danger`·`caution`만 정규화 URL·체인·설명을 남기고, 그 외는 해시와 판정만 |
| DB | Supabase (PostgreSQL) |
| 스크린샷 워커 | **Sprint 4로 미룸** — 별도 인프라가 필요해 스프린트를 통째로 먹을 위험 |
| 신고 남용 방지 | Cloudflare Turnstile + rate limit (IP당 5회/시간) |

**작업 순서를 뒤집는다.** 15번(약관·이의제기)을 먼저 한다. 신고 기능을 열어두고
나중에 약관을 붙이면 그 사이 들어온 신고는 근거 없이 수집한 것이 되고,
이의제기 창구는 신고 기능과 **같은 날** 열려 있어야 한다.

15. **이용약관·개인정보처리방침·이의제기 폼** — 신고 기능보다 먼저.
    개인정보처리방침에 반드시 적을 것: (a) 위험 판정된 주소만 원문 보관한다는 사실,
    (b) Safe Browsing 조회 때 검사한 주소가 구글로 전송된다는 사실.
16. ✅ **저장 계층** — 두 곳으로 나눈다. 목적이 다르고 프라이버시 무게가 다르다.
    - **Redis (6시간 TTL)**: 전체 결과 캐시. 자동 소멸하므로 깨끗한 검사도 담을 수 있다
    - **PostgreSQL (영구)**: `danger`·`caution`만 원문 포함. 신고 처리·관리자 검토·이의제기 대응에 필요
    - **공유 링크(`/s/{shortId}`)는 사용자가 "공유하기"를 누른 순간에만 레코드를 만든다.**
      깨끗한 결과도 공유할 수 있어야 하는데, 그때는 사용자가 저장에 동의한 것이므로 문제없다
17. ✅ **신고 기능 + 관리자 검토 화면** — 임계치를 넘겨도 자동 노출하지 않고 검토 큐로 보낸다.
    판정과 신고는 화면에서 분리해 표시한다 (CLAUDE.md 8).
18. ✅ **공유 카드 OG 이미지** (`@vercel/og`) + `/s/{shortId}` 라우트.
    카드에 실제 URL을 클릭 가능한 형태로 넣지 않는다.

### Sprint 4 — 스크린샷 + 앱 전환 (2~3주)

19. **스크린샷 워커** (격리 컨테이너, Playwright) — Sprint 3에서 이월.
    악성 URL에 실제로 접속하는 주체이므로 메인 인프라와 완전히 격리하고,
    DB 쓰기 권한과 자격증명을 주지 않는다.
20. Capacitor 셋업, 웹 빌드 래핑
21. **Android 공유 인텐트 수신** (`android.intent.action.SEND`)
22. **iOS Share Extension** (Swift 소량 필요 — 여기만 네이티브 코드)
23. 클립보드 감지, 로컬 검사 기록
24. 스토어 등록 (심사 반려 대비해 공유 확장 기능을 스크린샷·설명에 강조)

### Phase 2 이후

21. 앱 판별 (스토어 링크/패키지명 조회)
22. MobSF self-host 연동 검토

---

## 9. MVP 성공 기준

기능 완성보다 아래를 먼저 확인할 것:

- 검사 완료까지 **8초 이내** (초과 시 이탈)
- 위험 사이트 10건 테스트 시 최소 8건 위험/주의 판정
- **정상 사이트 20건 테스트 시 오탐 0건** ← 오탐 1건이 신뢰를 무너뜨림. 재현율보다 정밀도 우선
- 결과 화면을 본 사람 중 공유 카드 생성 비율 (바이럴 지표)

---

## 10. 열어둔 결정 사항

1. **로그인 도입 시점** — MVP는 익명. 검사 기록 서버 동기화가 필요해지는 시점에 소셜 로그인(카카오) 도입
2. **수익 모델** — 광고는 이 서비스의 신뢰도와 상충(보안 서비스에 광고가 붙으면 사용자가 그 광고를 의심함). B2B API 판매 또는 후원 모델 검토
3. **스크린샷 저장 여부** — 저장 시 개인정보·저작권 이슈. MVP에선 TTL 24시간 후 자동 삭제로 시작
4. **VirusTotal 비중** — 무료 4회/분 제한이 병목. 실시간 경로에서 빼고 비동기 보강용으로만 사용
5. **DB 도입 시점** — Sprint 1은 무상태(stateless). Sprint 3의 신고 기능부터 PostgreSQL 필요
6. ~~**정식 대체 스토어 취급**~~ — 2026-08-15 확정: F-Droid·원스토어·삼성 갤럭시스토어
   공식 도메인만 좁게 예외(`caution`). 그 외는 전부 `danger` 유지
7. **TLS 인증서 검증 정책** — 현재 스캐너는 `rejectUnauthorized: false`로 접속한다.
   인증서가 깨진 사이트도 검사해야 하고 그 사실 자체가 S6 시그널이기 때문.
   S6 구현 시 별도 TLS 프로브로 인증서 정보를 따로 수집하는 방식으로 갈 것
